// Project Euler 12
// What is the value of the first triangle number to have over five hundred divisors?

#include <iostream>
#include <math.h>
#include <vector>
#include <cstdlib>

std::vector<int> factors;

int main() {
  long N, triangle, counter;
  //while (std::count(factors.begin(), factors.end()) <= 500) { //need length not count
  //WINS.push_back (rollnumber); //add to vector
  //}


  return 0;
}
